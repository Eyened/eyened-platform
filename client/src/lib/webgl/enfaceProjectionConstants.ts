/**
 * Synthetic layer index for Binary / DualBitMask / Probability (single-layer
 * projection). Distinct from BACKGROUND_SUBFEATURE_INDEX (= 0) in
 * subfeatureBits — that skips the MC/ML background class. Same numeric value
 * by coincidence; do not conflate the two.
 */
export const SIMPLE_ENFACE_FEATURE_INDEX = 0;
