*.env
*.zip
*.pyc
notebooks/hrfav/**/*
notebooks/schema.json
node_modules/**/*
extensions.ts
client/src/lib/extensions.ts
storage/**/*
images/**/*
cookies.txt
.vscode/settings.json
client/src/lib/extensions.ts
TODO.md
orm/migrations/transfer-config.yml
orm/migrations/transfer-config.*.yml
# Regenerated on demand by orm/migrations/generate_latest_migration_sql.sh;
# a committed copy goes stale against the revision chain and misleads.
orm/migrations/sql/
segmentations.zarr/
**/.ipynb_checkpoints/**
.cursor/
client/src/lib/extensions.ts
venv
trash
scratch.txt

# Removed stacks: an older checkout can still hold untracked files here (a
# venv, .env.*) that no other rule matches.
dev/
docker/
database/

# Graphify (Claude Code) - regenerable knowledge graph, rebuild with `graphify update .`
graphify-out/

# Claude Code local config and agent working files
.claude/
.superpowers/
CLAUDE.md
docs/superpowers/

# deploy/ — secrets and generated artifacts
deploy/.env
deploy/.env.*
!deploy/.env.example
deploy/compose.storage.yaml
deploy/backups
deploy/snapshots/

# Agent / superpowers working docs (local only)
.agents/

.coverage
coverage.xml
htmlcov/
client/coverage/
# diff-cover writes its markdown report to the cwd, not into coverage/.
diff-cover.md
client/diff-cover-client.md
